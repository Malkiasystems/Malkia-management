This bundle deploys to Malkia-management (MalkiaOS) ONLY.
Do not copy any file in here into AtlasOS / Tarakimu.

SESSION BUNDLE 27 Aug 2026. Supersedes the earlier
malkia-importorder-shipment-doublepost-fix.zip from this session.
Deploy this one; all three files go together.

Contents:
  src/pages/vouchers/ImportOrder.tsx    (shipment double-post fix)
  src/pages/SalesDayBook.tsx            (expense visibility fix)
  src/lib/salesDayBookExport.ts         (expense visibility fix)

Migrations: NONE. Code-only deploy.
Data cleanup for IMP-10-0004 already applied directly to the DB via
MCP earlier today (F4 reversed as JV-FIX-IMP-10-0004-V, duplicate
payment row + shipments #2/#4 deleted, total_freight_tzs resynced).

CHANGE 1 — ImportOrder.tsx (Add Shipment double-post):
  0. Shipment numbering now uses MAX(shipment_number)+1 fetched fresh
     from the DB instead of row count+1. Count-based numbering would
     reissue #3 on IMP-10-0004 after the cleanup deleted rows #2 and #4.
  1. shipSaving in-flight guard: early return + button disabled with
     'Creating...' label while the flow posts. Fix for the triple-click
     that created shipments #2/#3/#4 and the duplicate TZS 741,150
     freight payment.
  2. On failure, order detail reloads before the modal lets you retry,
     so remaining-to-ship validation runs against real DB state.
  3. After a freight-paid-now payment, import_orders.total_freight_tzs
     is recomputed from import_payments. Previously this path never
     updated the stored column, so ImportRegister understated freight.

CHANGE 2 — SalesDayBook.tsx + salesDayBookExport.ts (expenses):
  1. Expense data is fetched ONLY for users with accounting.view (or
     super admin). Sales-only users no longer get expense vouchers in
     browser memory, the network tab, or any export.
  2. bank_payment vouchers are excluded from the Day Book entirely, for
     everyone. Back-office bank spends live in the Expense Register and
     vouchers pages; the Day Book now carries only cash_payment and
     petty_cash (till-relevant).
  3. Summary PDF: for gated viewers the Expenses stat prints
     'Restricted' and the Net Position stat is omitted, so a manager
     reading a cashier's export cannot mistake hidden for zero. Section
     retitled 'Cash & Petty Expenses' to reflect the narrower scope.
     Veto the retitle if you want the old wording.

NOT in this bundle (agreed for a later session):
  - RLS on vouchers is still USING(true): any authenticated user can
    read all vouchers via the REST API directly. The real lock needs a
    has_permission() helper + policy + full impact sweep. This bundle
    is the UI/UX layer only.
